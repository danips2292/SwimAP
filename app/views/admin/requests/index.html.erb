<h1>Solicitudes</h1>

<table>
	<% @users.each do |user| %>
		<tr>
			<td><%= user.full_name %></td>
			<td> <%= button_to( 'Acceptar', action: 'accept', id: user.id) %> </td>
			<td> <%= button_to( 'Rechazar', action: 'reject', id: user.id) %> </td>
		</tr>
	<%end%>
</table>