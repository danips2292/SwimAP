
<body>
  <div class="navbar navbar-inverse">
    <div class="container">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Videos', admin_videos_path %></li>
        <li><%= link_to 'Add Video',  new_admin_video_path %></li>
      </ul>
    </div>
  </div>

  <div class="container">
    <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %>">
        <%= value %>
      </div>
    <% end %>

    <%= yield %>
  </div>
</body>